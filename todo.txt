# Current Work Inventory

This file records the code currently in the repository and working tree before stashing.

## Already Committed: Personal Pickup (`934607c`)

- [ ] `PICKUP` payment method exists in backend, frontend and fresh database schemas.
- [ ] Checkout offers personal pickup at Spodnje Stranje 33.
- [ ] Pickup has EUR 0 delivery, skips GLS labels and omits Minimax shipping.
- [ ] Customer and owner emails describe pickup correctly.
- [ ] Admin orders show "Osebni prevzem".
- [ ] Existing databases need `PICKUP` added to `orders.payment_method`.

## Currently Uncommitted: EUR 150 Free Shipping

- [ ] Delivery costs EUR 5.99 below EUR 150 and EUR 0 at EUR 150 or above.
- [ ] The backend uses integer cents and is authoritative for the order total.
- [ ] The frontend shows the same EUR 150 calculation and free-delivery message.
- [ ] Minimax includes shipping only for delivery orders below EUR 150, at the same EUR 5.99 price.
- [ ] Customer and owner emails calculate/display the same shipping amount.
- [ ] Test delivery below EUR 150, exactly EUR 150 and above EUR 150.

## Currently Uncommitted: Checkout Security And Reliability

- [ ] Checkout rate limit: 10 order submissions per IP per 15 minutes.
- [ ] Logged-out checkout using an existing registered email returns an error.
- [ ] Minimax failure keeps the order and returns a successful response with `MINIMAX_INVOICE_FAILED` warning.
- [ ] GLS, owner-email and customer-email failures are returned as checkout warnings.
- [ ] UPN customer email is skipped when an invoice/PDF cannot be made.
- [ ] Minimax authorization headers are no longer written to logs.
- [ ] Open issue: guest profiles can still be overwritten by anyone using the guest email.
- [ ] Open issue: owner email returning `success: false` is not added to checkout warnings.
- [ ] Open issue: full Minimax invoice response and headers are still logged.

## Currently Uncommitted: Database Compatibility

- [ ] Startup migration adds `PICKUP` to existing `orders.payment_method` enums.
- [ ] Startup migration adds missing `products.display_order`, backfills it from product IDs and adds its index.
- [ ] Open decision: `display_order = 0` is currently overwritten with the product ID at every backend startup.

## Currently Uncommitted: TypeScript And UI Cleanup

- [ ] Payment/status/checkout strings are replaced with typed constants and request/response types.
- [ ] Removed broad `any` and `@ts-nocheck` usage from checkout, GLS, Minimax and mail code.
- [ ] GLS detects an empty label response.
- [ ] Admin shows an unknown payment method as "Neznan način plačila".
- [ ] Header/logo/navigation were made smaller.
- [ ] Open issue: GLS missing-credentials warning was removed; TypeScript `!` does not validate configuration at runtime.

## Currently Uncommitted: Dependency Changes

- [ ] Backend: Nodemailer 7.0.10 -> 9.0.3 and lockfile security updates, including Axios, Express, Multer and express-rate-limit.
- [ ] Frontend: package lock updates Angular CLI 20.1.3 -> 20.3.32 and changes 3,182 lockfile lines; runtime Angular packages remain 20.1.3.
- [ ] Backend `dist` files were rebuilt to match the TypeScript source.

## Deferred Later

- [ ] Database transactions for guest/profile, orders and order items.
- [ ] Idempotency keys to stop duplicate order/invoice creation.
- [ ] CAPTCHA or other bot protection.
- [ ] Slovenian-only checkout addresses, email verification, additional admin tools and documentation.
